# Animated Sankey Flow - Dependencies
# ====================================

# Core (required)
matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0

# Optional - data sourcing in examples/
requests>=2.28.0      # SEC EDGAR / generic HTTP
yfinance>=0.2.0       # stock prices (time-series overlay, e.g. NVDA close)

# Optional - background music from a YouTube URL (animate(audio_url=...))
yt-dlp>=2024.1.0      # also needs ffmpeg on PATH

# Note: video/audio export requires FFmpeg installed and on your PATH.
