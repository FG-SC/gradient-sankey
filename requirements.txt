# gradient-sankey - core dependencies
# ====================================
# These are all you need to build and render animations.
# For a proper install (with optional extras) prefer:  pip install -e .[finance,audio]

matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0

# Optional extras (only needed by some examples) live in pyproject.toml:
#   [finance] -> requests, yfinance   (SEC EDGAR scrape + stock overlay)
#   [audio]   -> yt-dlp               (background music from a YouTube URL)
#
# Video/audio export also requires FFmpeg installed and on your PATH (system dependency).
